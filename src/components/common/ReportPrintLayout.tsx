import type * as React from 'react';import type { ClinicProfile } from '@/lib/types';import { formatDateID } from '@/lib/format';
export function ReportPrintLayout({clinic,title,children}:{clinic:ClinicProfile;title:string;children:React.ReactNode}){return <div className="print-card rounded-2xl border bg-white p-5"><div className="mb-4 border-b pb-3"><h2 className="text-xl font-bold">{clinic.name}</h2><p className="text-sm text-slate-500">{clinic.legalEntityName} · {clinic.address} · {clinic.phone}</p><h3 className="mt-3 text-lg font-semibold">{title}</h3><p className="text-xs text-slate-500">Dicetak {formatDateID(new Date().toISOString())}</p></div>{children}<div className="signature-grid hidden"><div className="signature-box">Dibuat Oleh</div><div className="signature-box">Diajukan Oleh</div><div className="signature-box">Disetujui Oleh</div><div className="signature-box">Mengetahui</div></div></div>}
