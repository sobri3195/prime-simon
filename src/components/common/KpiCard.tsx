import { Card, CardContent } from '../ui/basic';import type { LucideIcon } from 'lucide-react';
export function KpiCard({title,value,subtitle,icon:Icon,tone='blue'}:{title:string;value:string;subtitle?:string;icon:LucideIcon;tone?:'blue'|'green'|'red'|'amber'|'cyan'}){const color={blue:'bg-blue-50 text-blue-700',green:'bg-emerald-50 text-emerald-700',red:'bg-red-50 text-red-700',amber:'bg-amber-50 text-amber-700',cyan:'bg-cyan-50 text-cyan-700'}[tone];return <Card><CardContent className="flex items-start justify-between"><div><p className="text-sm text-slate-500">{title}</p><p className="mt-2 text-2xl font-bold text-slate-950">{value}</p>{subtitle&&<p className="mt-1 text-xs text-slate-500">{subtitle}</p>}</div><div className={`rounded-2xl p-3 ${color}`}><Icon size={20}/></div></CardContent></Card>}
