import { Download, Menu, RotateCcw, Search, ShieldCheck } from 'lucide-react';import { Button, Input, Badge } from '../ui/basic';import { exportAllData, resetDemoData } from '@/lib/storage';import type { ClinicProfile, Settings } from '@/lib/types';
const monthName=(m:number)=>new Intl.DateTimeFormat('id-ID',{month:'long'}).format(new Date(2026,m-1,1));
export function Topbar({title,onMenu,onReset,profile,settings}:{title:string;onMenu:()=>void;onReset:()=>void;profile:ClinicProfile;settings:Settings}){const quickExport=()=>{const data=JSON.stringify(exportAllData(),null,2);const blob=new Blob([data],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`kumpc-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url)};return <header className="topbar sticky top-0 z-20 flex h-16 items-center justify-between border-b border-slate-200 bg-white/90 px-4 backdrop-blur"><div className="flex items-center gap-3"><Button variant="ghost" className="lg:hidden" onClick={onMenu}><Menu size={18}/></Button><div><p className="text-sm text-slate-500">{profile.name}</p><h2 className="font-semibold text-slate-900">{title}</h2></div></div><div className="hidden items-center gap-2 md:flex"><Badge variant="outline">Periode {monthName(settings.activeMonth??settings.defaultMonth)} {settings.activeYear??settings.defaultYear}</Badge><Badge variant="green"><ShieldCheck size={13}/> Tersimpan lokal</Badge><div className="relative"><Search className="absolute left-3 top-2.5 text-slate-400" size={16}/><Input placeholder="Cari modul..." className="w-52 pl-9"/></div><Button variant="outline" onClick={quickExport}><Download size={16}/>Quick Export</Button><Button variant="outline" onClick={()=>{resetDemoData();onReset()}}><RotateCcw size={16}/>Reset Demo</Button></div></header>}
