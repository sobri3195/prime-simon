export const romanMonths=['','I','II','III','IV','V','VI','VII','VIII','IX','X','XI','XII'];
export function generateDocumentNumber(kind:string,date=new Date(),sequence=1,opts:{category?:string;clinicCode?:string;financeCode?:string;style?:'request'|'voucher'|'cash'|'payment'}={}){const m=romanMonths[date.getMonth()+1],y=date.getFullYear(),clinic=opts.clinicCode||'KUMPC',finance=opts.financeCode||'KEU';if(opts.style==='voucher')return `NO. ${String(sequence).padStart(2,'0')}/${kind}/${m}/${y}`;if(opts.style==='cash')return `${String(sequence).padStart(3,'0')}/${clinic}-${finance}/${kind}/${m}/${y}`;if(opts.style==='payment')return `${String(sequence).padStart(4,'0')}/${finance}-${clinic}/${m}/${y}`;return `${String(sequence).padStart(3,'0')}/${opts.category||kind}/${clinic}-${finance}/${m}/${y}`}
