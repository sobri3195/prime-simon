import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';import { ChartCard } from '@/components/common/ChartCard';import { DataTable } from '@/components/common/DataTable';import { PageHeader } from '@/components/common/PageHeader';import { doctorSalesRanking } from '@/lib/calculations';import { formatPercent, formatRupiah } from '@/lib/format';import type { Doctor, DoctorFee, RevenueTransaction } from '@/lib/types';
export function DoctorSalesRanking({revenue,doctors,fees}:{revenue:RevenueTransaction[];doctors:Doctor[];fees:DoctorFee[]}){const rows=doctorSalesRanking(revenue,doctors,fees);return <div><PageHeader title="Laporan Pendapatan Per Dokter" description="Ranking dokter, total pendapatan, dan persentase kontribusi."/><ChartCard title="Top Dokter"><ResponsiveContainer height={260}><BarChart data={rows.slice(0,10)}><XAxis dataKey="doctorName" hide/><YAxis/><Tooltip formatter={v=>formatRupiah(Number(v))}/><Bar dataKey="total" fill="#0ea5e9"/></BarChart></ResponsiveContainer></ChartCard><div className="mt-4"><DataTable rows={rows.map(r=>({...r,id:r.doctorId}))} columns={[{key:'rank',header:'Rank',cell:r=>r.rank},{key:'doctor',header:'Dokter',cell:r=>r.doctorName},{key:'total',header:'Total',cell:r=>formatRupiah(r.total)},{key:'pct',header:'Kontribusi',cell:r=>formatPercent(r.percentage)}]}/></div></div>}
