import { DataTable } from '@/components/common/DataTable';import { PageHeader } from '@/components/common/PageHeader';import { formatRupiah } from '@/lib/format';import type { PettyCashRequest } from '@/lib/types';
export function PettyCashRequestPage({rows}:{rows:PettyCashRequest[]}){return <div><PageHeader title="Pengajuan Pengisian Kas Kecil" description="Saldo awal, debet/kredit, saldo berjalan, pembulatan, dan top up diminta."/><DataTable rows={rows} columns={[{key:'date',header:'Tanggal',cell:r=>r.requestDate},{key:'no',header:'Nomor',cell:r=>r.requestNo},{key:'open',header:'Saldo Awal',cell:r=>formatRupiah(r.openingBalance)},{key:'end',header:'Saldo Akhir',cell:r=>formatRupiah(r.endingBalance)},{key:'topup',header:'Top Up',cell:r=>formatRupiah(r.roundedTopUpAmount)},{key:'status',header:'Status',cell:r=>r.status}]}/></div>}
