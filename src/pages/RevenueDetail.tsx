import { Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';import { ChartCard } from '@/components/common/ChartCard';import { FilterBar } from '@/components/common/FilterBar';import { DataTable } from '@/components/common/DataTable';import { PageHeader } from '@/components/common/PageHeader';import { Select } from '@/components/ui/basic';import { groupSum } from '@/lib/calculations';import { formatRupiah } from '@/lib/format';import type { RevenueTransaction } from '@/lib/types';
export function RevenueDetail({rows}:{rows:RevenueTransaction[]}){const byPayer=groupSum(rows,r=>r.payerType,r=>r.netAmount),byCat=groupSum(rows,r=>r.serviceCategory,r=>r.netAmount);return <div><PageHeader title="Laporan Pendapatan Detail" description="Filter tanggal, payer, kategori layanan, grafik dan detail kuantiti/tarif/diskon/total."/><FilterBar><Select><option>Semua Payer</option></Select><Select><option>Semua Kategori</option></Select></FilterBar><div className="mb-4 grid gap-4 lg:grid-cols-2"><ChartCard title="By Payer"><ResponsiveContainer height={220}><BarChart data={byPayer}><XAxis dataKey="name"/><YAxis/><Tooltip formatter={v=>formatRupiah(Number(v))}/><Bar dataKey="value" fill="#2563eb"/></BarChart></ResponsiveContainer></ChartCard><ChartCard title="By Kategori Layanan"><ResponsiveContainer height={220}><BarChart data={byCat}><XAxis dataKey="name" hide/><YAxis/><Tooltip formatter={v=>formatRupiah(Number(v))}/><Bar dataKey="value" fill="#06b6d4"/></BarChart></ResponsiveContainer></ChartCard></div><DataTable rows={rows} columns={[{key:'date',header:'Tanggal',cell:r=>r.date},{key:'uraian',header:'Uraian',cell:r=>r.serviceName},{key:'qty',header:'Kuantiti',cell:r=>r.quantity},{key:'tariff',header:'Tarif',cell:r=>formatRupiah(r.tariff)},{key:'gross',header:'Pendapatan',cell:r=>formatRupiah(r.grossAmount),total:rs=>formatRupiah(rs.reduce((a,b)=>a+b.grossAmount,0))},{key:'discount',header:'Diskon',cell:r=>formatRupiah(r.discount)},{key:'net',header:'Total',cell:r=>formatRupiah(r.netAmount),total:rs=>formatRupiah(rs.reduce((a,b)=>a+b.netAmount,0))},{key:'payer',header:'Payer',cell:r=>r.payerName}]}/></div>}
