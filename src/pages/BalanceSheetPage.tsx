import { PageHeader } from '@/components/common/PageHeader';import { PlaceholderFinancial } from './moduleHelpers';import type { APItem, ARItem, FixedAsset, InventoryItem } from '@/lib/types';
export function BalanceSheetPage({ar,ap,assets,inventory}:{ar:ARItem[];ap:APItem[];assets:FixedAsset[];inventory:InventoryItem[]}){const aset=ar.reduce((a,b)=>a+b.outstandingAmount,0)+assets.reduce((a,b)=>a+b.bookValue,0)+inventory.reduce((a,b)=>a+b.openingAmount,0);const liab=ap.reduce((a,b)=>a+b.outstandingAmount,0);return <div><PageHeader title="Neraca" description="Aset, kewajiban, dan ekuitas ringkas dari data lokal."/><PlaceholderFinancial title="Neraca" rows={[{name:'Aset',amount:aset},{name:'Kewajiban',amount:liab},{name:'Ekuitas',amount:aset-liab}]}/></div>}
